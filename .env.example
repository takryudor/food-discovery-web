# ==============================================================================
# FRONTEND CONFIGURATION
# ==============================================================================
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Supabase Auth (JWT gửi kèm mọi request backend qua Authorization: Bearer)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Map (Phase 1 — clean map / compliance)
# osm = raster (Carto Voyager mặc định trong code). goong = nền vector Goong (cần key).
NEXT_PUBLIC_MAP_PROVIDER=osm

# Goong vector basemap — bắt buộc khi NEXT_PUBLIC_MAP_PROVIDER=goong
NEXT_PUBLIC_GOONG_MAPTILES_KEY=

# Tuỳ chọn: URL style JSON Goong (mặc định goong_map_web.json)
# NEXT_PUBLIC_GOONG_STYLE_URL=https://tiles.goong.io/assets/goong_map_web.json

# ==============================================================================
# BACKEND CONFIGURATION
# ==============================================================================
APP_NAME=FoOdyssey API
APP_ENV=development
API_V1_PREFIX=/api/v1
CORS_ORIGINS=["http://localhost:3000"]

# Database URL for Docker Compose (Postgres service name)
DATABASE_URL=postgresql+psycopg://foodyssey:foodyssey@postgres:5432/foodyssey

# Cache Configuration
FILTERS_CACHE_TTL_SECONDS=300

# AI Configuration (Groq)
GROQ_API_KEY=

# Authentication (Supabase)
# URL dùng để lấy JWKS (ES256). Có thể dùng NEXT_PUBLIC_SUPABASE_URL nếu chỉ có một biến.
SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_JWT_SECRET=
SUPABASE_JWT_ALGORITHM=HS256

# Maps Configuration (Google)
GOOGLE_MAPS_API_KEY=

# ==============================================================================
# INFRASTRUCTURE (DOCKER)
# ==============================================================================
POSTGRES_DB=foodyssey
POSTGRES_USER=foodyssey
POSTGRES_PASSWORD=foodyssey
